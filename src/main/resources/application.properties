# PostgreSQL Configuration (Supabase)
spring.datasource.url=jdbc:postgresql://db.njggicxkxnzrajunmomw.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Cupper.2026!
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Initial Data
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true

# Admin Credentials
admin.username=Quiniela
admin.password=Cupper10

# Danger Zone Credentials
admin.delete.password=Cupper2026

# Server Port (Railway/Render asigna el puerto mediante la variable de entorno PORT)
server.port=${PORT:8080}
