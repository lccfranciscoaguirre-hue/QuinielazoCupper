# PostgreSQL Configuration (Neon.tech / Supabase / Railway)
# Usamos una URL más simple y estándar que es más compatible.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-round-shadow-aqjvkqv2-pooler.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_6ODWlUZxs5bX}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Initial Data
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true

# Admin Credentials
admin.username=Quiniela
admin.password=Cupper10

# Danger Zone Credentials
admin.delete.password=Cupper2026

# Server Port (Railway/Render asigna el puerto mediante la variable de entorno PORT)
server.port=${PORT:8080}